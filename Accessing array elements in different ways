#include<stdio.h>
int main()
{
	int num[] = {24,34,12,44,56,17};
	for(int i=0;i<6;i++)
	{
		printf("\naddress = %u",&num[i]);
		printf("\nelement = %d",num[i]);
		printf("\nelement = %d",*(num+i));
		printf("\nelememt = %d",*(i+num));
		printf("\nelement = %d",i[num]);
		
	}
	
}
